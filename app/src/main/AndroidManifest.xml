<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="l.b.v.udacitytestapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".mainMenu.MenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".lesson1.Lesson1Activity" />
        <activity android:name=".lesson2.part1.AboutMeActivity" />
        <activity android:name=".lesson2.part2.ColorMyViewsActivity" />
        <activity android:name=".lesson3Navigation.AndroidTriviaActivity"></activity>
    </application>

</manifest>